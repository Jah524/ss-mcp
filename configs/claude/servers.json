{
  "mcpServers": {
    "example-server": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "example_server.server"],
      "env": {
        "MCP_HOME": "${MCP_HOME}"
      }
    }
  }
}
